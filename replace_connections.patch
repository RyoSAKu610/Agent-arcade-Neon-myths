--- "neon-mythos-boosted (1).html"
+++ "neon-mythos-boosted (1).html"
@@ -3689,24 +3689,29 @@
     runAI(setAgents);
     runAI(setResi);
-    if (tick % 10 === 0) {
-      const workers = [...agents, ...resi].filter(a => a.state === "work");
-      const newConns = [];
-      for (let i = 0; i < Math.min(3, workers.length - 1); i++) {
-        if (workers.length >= 2) {
-          const a = workers[Math.floor(Math.random() * workers.length)];
-          const b = workers[Math.floor(Math.random() * workers.length)];
-          if (a.id !== b.id) newConns.push({
-            id: `${a.id}-${b.id}-${tick}`,
-            from: a,
-            to: b,
-            time: 0
-          });
-        }
-      }
-      setConnections(prev => [...prev, ...newConns].slice(-8));
-    }
-    setConnections(prev => prev.map(c => ({
-      ...c,
-      time: c.time + 1
-    })).filter(c => c.time < 30));
+    setConnections(prev => {
+      let next = prev.reduce((acc, c) => {
+        if (c.time + 1 < 30) acc.push({ ...c, time: c.time + 1 });
+        return acc;
+      }, []);
+
+      if (tick % 10 === 0) {
+        const workers = [...agents, ...resi].filter(a => a.state === "work");
+        const newConns = [];
+        for (let i = 0; i < Math.min(3, workers.length - 1); i++) {
+          if (workers.length >= 2) {
+            const a = workers[Math.floor(Math.random() * workers.length)];
+            const b = workers[Math.floor(Math.random() * workers.length)];
+            if (a.id !== b.id) newConns.push({
+              id: `${a.id}-${b.id}-${tick}`,
+              from: a,
+              to: b,
+              time: 0
+            });
+          }
+        }
+        next = [...next, ...newConns].slice(-8);
+      }
+      return next;
+    });
   }, [tick, paused, showAvSel, showLangSel]);
